{
  "dependencies": {
    "@formkit/auto-animate": "^0.9.0",
    "imask": "^7.6.1",
    "playwright": "^1.57.0",
    "toastify-js": "^1.12.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "autoprefixer": "^10.4.23",
    "daisyui": "^4.12.23",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "vite": "^7.3.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "powershell -Command \"Remove-Item -Path 'public_html/*' -Recurse -Force -ErrorAction SilentlyContinue; Copy-Item -Path 'dist/*' -Destination 'public_html/' -Recurse -Force\"",
    "deploy": "npm run build && npm run sync"
  }
}